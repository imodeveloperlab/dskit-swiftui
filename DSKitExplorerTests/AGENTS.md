# Agent Instructions

## Purpose
- Snapshot tests for DSKitExplorer screens using Point-Free `SnapshotTesting`.

## Notes
- Goldens live under `__Snapshots__/`.
- Tests are enumerated in `DSKitExplorerTests.swift`.

## Run / Test in Isolation
- Run tests via `DSKitExplorer` scheme and `DSKitExplorerTests/DSKitExplorer.xctestplan`.
