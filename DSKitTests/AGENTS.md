# Agent Instructions

## Purpose
- Snapshot tests for DSKit components using Point-Free `SnapshotTesting`.

## Notes
- Goldens live under `__Snapshots__/`.
- Test plan is owned by `DSKitExplorerTests/DSKitExplorer.xctestplan`.

## Run / Test in Isolation
- Run tests via the `DSKitExplorer` scheme (includes `DSKitTests`).
